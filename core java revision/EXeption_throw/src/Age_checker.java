public class Age_checker extends Exception{
     // we are passing value string in constructor
    // super is doing it is overiding expextion class and goin to parent class cntructor

    Age_checker(String string){
        super(string);
    }
}
