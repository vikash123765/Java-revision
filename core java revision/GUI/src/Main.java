import javax.swing.*;
import java.awt.*;
import java.util.jar.JarEntry;

public class Main {
    public static void main(String[] args) {

        // Jframe = a GUi window to add components to

       /* JFrame frame = new JFrame(); // creat new frame
        frame.setTitle("JFrame title goes here"); // this sets title of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exists out of the application
        frame.setResizable(false); // orevent frame from being reSIZABLR
        frame.setSize(420,420); // this sets the x dimension and the y dimension of our frame
        frame.setVisible(true);    // makes frame visable

        ImageIcon image = new ImageIcon("Mitt projekt-1.png"); // this will create an image icon
         frame.setIconImage(image.getImage()); // change icon of frame

        // frame.getContentPane().setBackground(Color.green); // default system colors

        frame.getContentPane().setBackground(new Color(2,45,68)); // custom  color*/

    MyFrame myFrame = new MyFrame();



    }
}