public class Main {
    public static void main(String[] args) {
      // absracti classes can not be directly instantiated but can have subclass, abstract method are declared without implementation

      Car car = new Car();

    // we can not create instance because its to vauge:  Veichle veichle= new Veichle();

    car.go();
    }
}